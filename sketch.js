const density = ""

//create mapping to render the average brightness value 

try{
    let rendered = await art.font("Family Thyme", 'green', 'true').completed()
    //rendered is the ascii
}catch(err){
    //err is an error
}